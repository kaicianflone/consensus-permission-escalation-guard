# consensus-permission-escalation-guard

Pre-execution governance for IAM and privilege escalation requests.
Returns `ALLOW | BLOCK | REQUIRE_REWRITE` with persona-weighted consensus, strict schema checks, idempotency, and board-native audit artifacts.
